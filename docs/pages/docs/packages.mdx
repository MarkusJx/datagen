# Packages

`datagen` consists of multiple packages:

- Node.js packages:
  - [`@datagen/node`](packages/node) - Node.js bindings for `datagen`
  - [`@datagen/types`](packages/nodeTypes) - TypeScript types for `datagen`
