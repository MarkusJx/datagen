import GlobalContext from '../components/util/GlobalContext';

export default function App({ Component, pageProps }) {
  return (
    <GlobalContext>
      <Component {...pageProps} />
    </GlobalContext>
  );
}
