[package]
name = "progress-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib", "rlib"]

[dependencies]
datagen-rs = { path = "../../datagen-rs", features = ["generate", "map_schema"] }
serde_json = "1.0"
rand = "0.8"

[features]
default = ["plugin"]
plugin = []
